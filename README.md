Ecomersce API FULL TEST SUITE


TODO:
- Put and POST for all 3 tables add include and specific DTO classes to stop cycling
  - currently only return objects so relationships are only viewable on GET requests
